# _processing

Draft or unconfirmed vendor docs — applied but not yet accepted/credited, or
still being researched. Move the file up to `../` once the program is
confirmed active; delete it if the lead doesn't pan out.
